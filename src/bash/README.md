# Original Bash version

`POSIX` and `ShellCheck` compliant shell script.

Just make it executable and run with sudo. Runs on any Unices: Linux, MacOS, BSD...

```bash
chown +x sshlog
sudo ./sshlog -a
```
