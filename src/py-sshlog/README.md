# `sshlog` in Python

`Python 3.6+` + `PEP8` style-guide and relies strictly on the **Python Standard Library**.

## Usage:

Make executable: `chown +x sshlog.py`

Then run with sudo: `sudo ./sshlog.py -a`
