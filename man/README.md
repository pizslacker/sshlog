# Linux man page

Use this directory to generate a Gzip-compressed manual page.

Create the man-page file with: `./make-man sshlog`

Copy the resulting man-page file `sshlog.1.gz` to `/usr/local/share/man/man1`.
